# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นาย อังคาร สกุลบุญดี **รหัส:** 68543210048-3 **Sec:** 1 **ชุด:** A

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 11:20 | B1 | ถามหลักการทำงานของ React state กับ useEffect ว่าทำไมข้อมูลถึงไม่อัปเดต และวิธีแก้ Warning เรื่อง key ใน list | อธิบายสาเหตุของบั๊ก เช่น ลืมใส่ key ใน map, การดึง state ผิด, และลืมกำหนด dependency ของ useEffect | นำหลักการมาไล่เช็คโค้ดทีละไฟล์ แก้ไขตามจุดต่างๆเพื่อทำให้ระบบทำงานได้|
| 13:40 | B2 | ถามวิธีเขียนฟังก์ชันกรองข้อมูล (filter) ให้ทำงานร่วมกันได้หลายเงื่อนไข ทั้งจากการพิมพ์ค้นหาและปุ่มสถานะ | แนะนำคอนเซปต์การใช้ `.filter()` ต่อกัน (Chain filter) เพื่อแยก logic การทำงานแต่ละส่วนออกจากกัน | นำคอนเซปต์มาเปรียบเทียบกับโค้ดเดิม และปรับใช้เพื่อแยกการกรองข้อมูล ทำให้ค้นหาและกรองสถานะพร้อมกันได้ |
| 13:55 | B2 | ถามวิธีเช็คว่าถ้าข้อมูลจากการค้นหาว่างเปล่า จะให้แสดงข้อความแจ้งเตือนที่หน้าจอแทนรายการได้อย่างไร | แนะนำให้เช็คความยาวของข้อมูล (`requests.length === 0`) เพื่อ return ข้อความแทนการวาด UI รายการ | เพิ่มเงื่อนไขตรวจสอบในคอมโพเนนต์และปรับข้อความแจ้งเตือนให้ตรงกับโจทย์ |
| 14:10 | B3 | ถามแนวทางการซ่อน/แสดงปุ่มตามสถานะของข้อมูล และวิธีส่งฟังก์ชันการกดปุ่มจาก Dashboard ไปที่ Card | แนะนำวิธีเขียนเงื่อนไขแบบ `&&` สำหรับซ่อนปุ่ม และการส่งฟังก์ชันผ่าน prop ลงไปเป็นทอดๆ | นำรูปแบบเงื่อนไขมาใช้ครอบปุ่ม "ทำเสร็จ" และรับ prop ฟังก์ชันมาเรียกใช้ ทดสอบกดอัปเดตและรีเฟรชหน้าจอค่าไม่หาย |
| 14:30 | B4 | ถามวิธีสร้าง Component ใหม่เพื่อดักจับเงื่อนไขและแสดงข้อความ/สี พร้อมการทำ Fallback หากเจอค่าที่ไม่รู้จัก | ให้โครงสร้างการเขียน if-return แยกตามเงื่อนไข และแนะนำการกำหนดค่า default กรณี edge case | นำโครงสร้างมาสร้างไฟล์ `PriorityBadge.jsx` เขียนดักเงื่อนไขดึงคลาส CSS ตามโจทย์ และนำไปแทนที่ข้อความเดิมในการ์ด |

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** อังคาร สกุลบุญดี